<!DOCTYPE html>
<html>
  <head>
    <title>Invoice Generator</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="text-white bg-dark">
    <%= link_to image_tag('husky.svg', width: 100, class: 'm-3'), root_path %>

    <% if Current.user.present? %>
      <p class='text-right d-flex justify-content-end'>
      <%= button_to t('sign_out', scope: 'forms.buttons'), session_url(Current.user.id), method: :delete, class: "btn btn-danger"  %>
      </p>
    <% end %>
    <div class="container">
      <%= render 'shared/flash' %>
      <%= yield %>
    </div>
  </body>
</html>
