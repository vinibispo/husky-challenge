<%= form_with(model: session_model) do |f| %>
  <%= f.label :token %>
  <%= f.text_field :token %>
  <%= f.submit I18n.t(create_or_update(session_model), scope: 'forms.buttons.sessions') %>
<% end %>
