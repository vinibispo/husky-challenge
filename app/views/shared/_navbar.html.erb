<div class="d-flex justify-content-between align-items-center">
  <%= link_to image_tag('husky.svg', width: 100, class: 'm-3'), root_path %>

  <% if Current.user.present? %>
    <%= link_to t('sign_out', scope: 'forms.buttons'), session_url(Current.user.id), data: { 'turbo-method': :delete }, class: "btn btn-danger m-3"  %>
  <% end %>
</div>
